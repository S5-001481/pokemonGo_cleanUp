# Project guide change log

| Timestamp | Request | Actions | Verification | Result |
| --- | --- | --- | --- | --- |
| 2026-07-28 17:22 JST | Create the initial local-first Windows ADB capture project. | Built the package, CLI, error model, local PNG/JSON persistence, tests, Windows/Huawei guide, and Lite repo-docs capture walkthrough. | 16 pytest tests, Ruff, and mypy passed; CLI help and missing-ADB smoke checks passed; repo-docs validator reported 0 errors and the expected no-SHA warning. | Initial behavior model and evidence map created. Synced through uncommitted initial working tree; no Git commit identity is configured. |
| 2026-07-28 17:47 JST | Add a Simplified Chinese README. | Added `README.zh-CN.md` as a natural Chinese localization and linked both language versions from the root README. | Confirmed UTF-8 encoding and local link targets; repo-docs validator reported 0 errors and the expected no-SHA warning. | Chinese onboarding is available. Synced through uncommitted initial working tree; no Git commit identity is configured. |
| 2026-07-28 19:04 JST | Implement a guided three-screenshot scan for one Pokémon. | Added bilingual manual prompts, scan IDs, atomic summary/moves/appraisal persistence, progressive manifests, incomplete recovery, CLI options, an injectable ADB runner, tests, bilingual README guidance, and a guided walkthrough/evidence sync. | 23 pytest tests, Ruff, strict mypy, root/scan CLI help, and local UTF-8/link checks passed; repo-docs validator reported 0 errors and the expected no-SHA warning. | Guided local scanning is implemented without OCR, tap/swipe commands, gameplay automation, network/account access, or repository screenshots. Physical Huawei validation remains. Synced through uncommitted initial working tree; no Git commit identity is configured. |
| 2026-07-28 21:47 JST | Add recursive dataset validation, status reporting, manual ground-truth annotation, and a stable missing-`--guided` CI error. | Added full PNG decoding and dimension checks, strict Pydantic models, complete/incomplete/invalid classification, atomic UTF-8 annotation with overwrite protection and JSON input, synthetic tests, bilingual guides, CI dependency checking, and a four-pass repo-docs sync. | 45 pytest tests, Ruff, strict mypy, and `pip check` passed on local Python 3.12; all eight real local scans validated complete; repo-docs validation completed after the required module/glossary structure was added. | Dataset preparation is ready without OCR or automatic phone interaction. Synced through d1017e4 plus the current uncommitted working tree. |
| 2026-07-29 16:26 JST | Implement one safety-gated automatic Huawei Mate 30 scan. | Added centralized fixed coordinates, ADB tap/swipe/BACK wrappers, six page states, stable-screen waits, dry-run zero-input behavior, OCR-only appraisal targeting with transfer clearance, IV-bar confirmation, debug evidence, timeouts, incomplete recovery, CLI integration, focused fake-ADB tests, bilingual usage docs, and an automatic walkthrough/evidence sync. | 64 pytest tests, Ruff, strict mypy, pip check, Git whitespace checks, and CLI help passed; one existing real scan trio returned detail_summary, detail_moves, and appraisal_bars with IV 15/15/15; repo-docs validator reported 0 errors and 0 warnings. | The implementation is complete in source, but physical dry-run/live input remains unverified because no ADB device was connected. Synced through d1017e4 plus the current uncommitted working tree. |
| 2026-07-29 17:42 JST | Remove the unnecessary detail-page reverse scroll before opening the fixed Huawei menu. | Deleted `scroll_to_menu`; accepted either detail state before the fixed `(1244, 2772)` tap; added 500 ms OCR `action_menu` polling, a 10-second timeout, one gated retry, and per-attempt debug screenshots/state/timing logs. | All 65 tests, Ruff, strict mypy, `pip check`, Git whitespace checks, and repo-docs validation passed. A connected Mate 30 live run reached OCR-confirmed `action_menu` on the first tap at 0.99948 confidence and then stopped safely at the later existing `open_appraisal` stability timeout. | The requested menu-opening path is device-confirmed; complete appraisal progression remains a separate unresolved runtime issue. Local data stayed ignored. |
| 2026-07-29 18:18 JST | Add bounded batch scanning without changing the proven one-Pokémon flow. | Added a separate Mate 30 batch profile and service, the then-configured horizontal swipe, 500 ms/15-second identity polling, summary-before-IV classification, two-attempt ceiling, wrap stop, perceptual fingerprint, per-row atomic CSV, complete-manifest resume checks, CLI options, debug evidence, focused tests, bilingual docs, and repo-docs sync. | Source checks passed before final sync. Physical testing first rejected a black screen with zero input, then atomically saved 搗蛋小妖 CP318 and reached an OCR-confirmed 睡睡菇 CP431 summary; a false IV priority was fixed in the batch layer. | Batch persistence was device-confirmed; the direction assumption was corrected to left-only on 2026-07-29. The second complete row remained blocked by the then-unchanged one-scan move-anchor detector. All generated data remained ignored. |

| 2026-07-29 18:42 JST | Relax only the fixed Mate 30 `detail_moves` gate after an authorized second-row failure. | Kept the existing anchored OCR path and added a fallback that pairs a confident move name with its same-row right-side damage number; the “道館對戰＆團體戰” heading is now optional confidence evidence. Added focused positive and appraisal-negative tests and synced the automatic/batch evidence boundary. | 14 focused automation/batch tests, focused Ruff, and focused strict mypy passed; the final full suite passed 71 tests, Ruff, strict mypy, `pip check`, and repo-docs validation with 0 errors/warnings. Saved real move and appraisal screenshots respectively returned `detail_moves` and `unknown`. A live `--resume` run preserved 搗蛋小妖 CP318 as row 1, completed 睡睡菇 CP431 as row 2 with all four artifacts and a complete manifest, then stopped at `limit_reached`. | The old heading dependency is removed without changing menu, appraisal, IV, switching, or recognition code; the required two-row physical batch is complete. Synced through d1017e4 plus the current uncommitted working tree. |

| 2026-07-29 19:34 JST | Stop treating animated detail screens as stable and prevent adjacent resume duplicates. | Replaced only `scroll_to_moves` whole-screen stability with 500 ms/15-second `detail_moves` polling and a summary-only second swipe; added a fixed `(100,1550,1340,2300)` static ROI, last-row resume comparison, optional HP evidence, pre-scan switch, append guard, and required debug artifacts without changing menu/appraisal/IV/switch coordinates/atomic CSV code. | 20 focused tests and the full 77-test suite passed with Ruff, strict mypy, and `pip check`. Real local repeated summaries produced distance 0 while distinct examples produced 30–62. The first live resume precheck saw `unknown`, sent no input, and preserved the existing two-row CSV. A prepared bounded run then found 熔蟻獸 CP1591 already differed from the last 睡睡菇 row (static distance 60, threshold 8), sent no preliminary horizontal swipe, and reached `detail_moves` on poll 1 at 0.99701 without a stability artifact. It later stopped in the unchanged appraisal-dialogue limit and still preserved the two-row CSV. | Both safety fixes are source-, test-, and device-confirmed for the already-next resume path; the same-as-last preliminary-switch branch is focused-test-confirmed. Synced through the current working tree. |

| 2026-07-29 20:17 JST | Correct batch next-item switching to the device-confirmed left-only direction. | Replaced the old direction with a 600 ms `(1180,1500)` to `(260,1500)` left swipe and one 850 ms `(1300,1500)` to `(140,1500)` left retry; normal scanning and resume keep sharing `_switch_to_next`; change detection now accepts name, CP, or static fingerprint evidence; no right fallback exists. | Focused batch tests passed 9/9 and assert exact coordinates plus `start.x > end.x`; the full 78-test suite, Ruff, strict mypy, `pip check`, and repo-docs validation also passed. | The correction is minimal and leaves OCR, resume deduplication, one-scan automation, menu/appraisal/IV handling, and CSV persistence unchanged. Synced through the current working tree. |

| 2026-07-29 21:08 JST | Change only the appraisal-exit input from Android BACK to a center tap. | `exit_appraisal` now calls `self._adb.tap(serial,720,1560)` directly and records the same literal coordinates in the dry-run plan and actions log, without adding or reading an AppConfig or automation-config field; all waiting, state detection, retry, switching, OCR, IV, resume, and CSV behavior remains unchanged. | All 78 pytest tests passed; Ruff and strict mypy passed. No real device was accessed. | The automatic and batch walkthroughs describe the center tap. Synced through the current working tree. |

| 2026-07-29 21:40 JST | Prevent repeated appraisal-dialogue taps while IV bars are delayed. | Replaced the four-attempt loop with one `(1120,1660)` tap and a 500 ms poll for up to 30 seconds; dialogue/unknown states wait without input, unexpected states stop, timeout preserves `appraisal_wait_timeout.png`, and the existing `(720,1560)` exit tap remains unchanged. | Source editing only; no tests, linters, type checks, validators, or real-device actions were run at the user's request. | Waiting, state recognition, left switching, OCR, IV geometry, resume, and CSV logic outside this dialogue transition remain unchanged. Synced through the current working tree. |

| 2026-07-29 23:45 JST | Accept a bright-background detail summary when CP OCR is temporarily unavailable. | Added name+CP/name+HP summary evidence, a no-input 500 ms/15-second initial poll with last-valid HP fallback, and strict multi-preprocessing CP OCR that rejects candidates without a CP prefix. | Source inspection only; no tests, Ruff, mypy, repo-docs validator, or real-device action was performed at the user's request. | Bright CP glare no longer forces a valid name+HP detail page to `unknown`, while recognition never invents CP from HP or other numbers. Synced through the current uncommitted working tree. |
| 2026-08-25 JST | Add an opt-in automatic nickname suffix using recognized IVs. | Added `scan-auto-one --rename-with-iv` and the GUI checkbox. After a completed appraisal/recognition, the fixed 1440×3120 detail page opens the pencil editor, requires OCR-confirmed cancel and confirmation controls, clears the prior nickname to restore Pokémon GO's default Traditional Chinese name, reopens the editor, and appends `attack/defense/hp`. Added explicit ADB key/text wrappers and focused tests. Batch deliberately rejects the GUI checkbox and has no CLI option because its adjacent-summary identity guard depends on the pre-scan name. | Full pytest suite (79), Ruff, strict mypy, Git whitespace checks, and CLI help passed locally. No connected-device rename dialog was available, so live dialog OCR/coordinates remain unverified. | Single-scan renaming is an explicit, gated mutation; the default scan flow and batch safety boundary remain unchanged. Synced through 4a95dc252c0d96a4747d90e20561b19d50eb8ab1 plus the current uncommitted working tree. |
| 2026-08-25 14:27 JST | Diagnose the first live `--rename-with-iv` failure. | Inspected the incomplete manifest, recognition result, action log, state samples, and before/after screenshots. The scan and `15/14/15` recognition completed, but `(932,1690)` landed about 230 pixels below the visible pencil (approximately `(932,1460)`), so the nickname editor never opened. Updated the automatic walkthrough and evidence status; no behavior code was changed. | The two post-tap samples both remained `detail_summary`; the action log ends after the pencil tap, the manifest records `failed_step: "rename_with_iv"`, and no delete/text command appears. | The safety gate worked and preserved an honest partial run, but the nickname edit coordinate is miscalibrated and still requires a code correction plus live verification. Synced through 4a95dc252c0d96a4747d90e20561b19d50eb8ab1 plus the current uncommitted working tree. |
| 2026-08-25 14:35–15:16 JST | Recalibrate the nickname pencil, model the Huawei keyboard, and require half-width IV punctuation. | Changed the pencil from `(932,1690)` to `(932,1460)`. Added a separate `rename_keyboard` state that requires the editor title and OCR-derived right-side `確定`, clicks it near `(1248,1712)`, then requires the real `取消`/`OK` dialog and clicks `OK` near `(719,1665)`. Replaced `adb input text` for the IV suffix with digit keycodes and `KEYCODE_SLASH`, then believed those keyevents preserved half-width numbers and `/`; the 2026-08-26 correction below supersedes that conclusion. | The full 79-test suite, Ruff, strict mypy, and Git whitespace checks passed; saved real screenshots classified the keyboard and dialog targets. Live scan `20260825_150806_710137_981f9622c35648f8bb33eee96d48fb2a` completed the corrected two-pass rename to `呆火駝15/14/15`; scan `20260825_151358_160055_60784be4263347e3856fc959f66504d3` completed again with action keycodes `[8,12,76,8,11,76,8,12]`, a complete manifest, and the final detail screenshot. | The pencil, keyboard dismissal, real dialog confirmation, and default-name reset were device-confirmed. Character width was not actually proven because NFKC-normalized OCR hid the full-width distinction. Batch remained unchanged and excluded renaming at this point. Synced through 4a95dc252c0d96a4747d90e20561b19d50eb8ab1 plus the current uncommitted working tree. |
| 2026-08-26 ASCII nickname input correction | Correct the assumption that digit and slash keyevents guarantee half-width characters. | Replaced the per-character keyevent loop with one call to the existing `AdbClient.input_text()` for the complete ASCII IV suffix. Preserved OCR candidate width in the editor path and changed automatic, batch-transition, and resume verification to reject full-width digits or slashes instead of normalizing them through NFKC. Updated current documentation; the earlier keyevent entry remains as historical context, but its half-width conclusion is superseded. | All 98 tests passed, including ASCII command construction, one-call text input, width preservation, and rejection of `測試１５／１４／１３`; Ruff, strict mypy, and Git whitespace checks also passed. | `input text` is the requested input path, while width-sensitive OCR remains the fail-closed proof before any keyboard or game confirmation. Synced through the current uncommitted working tree. |
| 2026-08-26 10:21–10:24 JST | Diagnose the first physical `input_text()` retry. | Inspected scan `20260826_102152_537806_e166da7687744511bd0c9ef7d8534f99`, its manifest, recognition, action/state logs, keyboard screenshot, and read-only device input-method state. Confirmed one `input_text("10/14/4")` call became editor text `腕力10／14／4`. Queried Android 10 command availability without sending input or switching IMEs; neither `cmd clipboard` nor `cmd input` has a shell implementation. | The manifest is incomplete at `rename_with_iv`; the action log ends immediately after the text request and contains neither confirmation tap. The raw width-sensitive OCR candidate contains two full-width slashes. The active IME is Gboard `LatinIME`, while the visible spacebar shows its Pinyin layout. `git diff --check` passed after this guide sync. | The new safety gate works, but both per-key keyevents and ordinary `input text` are unsuitable under the current Pinyin layout. A future fix must verify a half-width-capable keyboard state and restore the user's original state, or use another exact commit-text channel. Synced through the current uncommitted working tree. |
| 2026-08-26 English-layout operating requirement | Document the user's chosen manual workaround instead of adding automatic keyboard switching. | Added a prominent rename-with-IV prerequisite to both root READMEs: manually switch Gboard to English before enabling the GUI option or CLI flag. Synchronized the living overview and automatic walkthrough to state that the program does not switch or restore keyboard layouts. No behavior code or tests changed. | Git whitespace checks passed; the bilingual instructions name both entry points and retain the existing fail-closed full-width guard. | English Gboard is now an explicit operator prerequisite for rename-enabled scans; Pinyin remains safe but cannot complete the rename because its full-width result is rejected. Synced through the current uncommitted working tree. |
| 2026-08-26 current-program README refresh | Update the public README from the implemented CLI, GUI, batch, rename, persistence, and safety behavior. | Expanded both root READMEs with GUI preparation and dry-run steps; current device/capture/guided/automatic/batch/read/dataset/annotation commands; total-row `--limit` and same-mode resume rules; optional rename artifacts and 16-column CSV evidence; incomplete-manifest, exit-15/16, Ctrl+C, and no-partial-row behavior. Kept the manual Gboard English prerequisite prominent. No program code changed. | Replayed every documented command name and option against current Typer `--help`; Git whitespace checks passed. | The bilingual public onboarding now describes the current program rather than only installation and the original capture layout. Synced through the current uncommitted working tree. |
| 2026-08-26 10:43–10:45 JST | Diagnose the latest `古月鳥` rename failure. | Inspected scan `20260826_104349_261861_15115ad2f606423a8064257e0d1e1b4c`, its manifest, recognition, action/state sequence, available artifacts, and two immediately preceding complete English-layout renames. No phone input or behavior code change was made. | At the time, exhaustive OCR stored CP121 and IV `15/15/13`; the user later corrected the real CP to 1217. The first pencil target `(943,1456)` and reset-to-default pass succeeded. The post-reset general summary reported `古月鳥`, CP121, and HP, but no second bounded wide-row pencil target was recorded. The action log contains no append tap or text, and no post-reset screenshot exists for a narrower pixel diagnosis. `git diff --check` passed. | This was a fail-closed transient wide-row OCR miss after reset, separate from the CP overwrite corrected by the later progressive-fallback entry. No incorrect nickname was saved. Synced through the current uncommitted working tree. |
| 2026-08-26 fixed nickname-row center | Stop depending on pencil or name-row OCR to open the nickname editor. | Added `(720,1460)` to the fixed Mate 30 automation profile and used it for both editor openings after the existing `detail_summary` gate. Removed the edit-target OCR protocol, implementation, tuning fields, and obsolete target tests; dry-run now prints the literal coordinate. Kept the post-tap editor-state gate, all delete/text/half-width checks, final wide nickname evidence, CP/HP/fingerprint transition, switch, wrap, and resume behavior unchanged. | All 94 tests, Ruff, strict mypy, and Git whitespace checks passed. Coverage includes the literal dry-run/action coordinate and a fixed-center tap that fails to open the editor, then sends no delete or text input. | Animated name-row OCR can no longer block the edit tap, while a wrong page or failed editor opening still stops before destructive nickname input. Physical fixed-center confirmation remains pending. Synced through a69f281. |
| 2026-08-26 GUI successful-scan counter | Show how many Pokémon have completed successfully in the current GUI run. | Added a bottom status value that resets for single/batch starts, becomes 1 only when a single scan exits 0, and polls the atomic batch CSV for real-time progress. Resume stores the initial data-row count and displays only the current run's additions. CSV parsing uses the standard reader so quoted embedded newlines do not inflate the count. | All 96 tests, Ruff, strict mypy, and Git whitespace checks passed. Focused coverage includes missing/header-only CSVs and quoted embedded newlines. | Failed items and historical resume rows are excluded from the displayed success count. Synced through a69f281 plus the current working tree. |
| 2026-08-26 visible GUI scan counter | Keep the successful-scan count visible in the requested settings-row position. | Moved the counter out of the trailing bottom status row into the open area to the right of `最多扫描数量`, increased the number font, and added the `只` unit. Current status remains in a separate row before the log; counting semantics are unchanged. | GUI helper tests, Ruff, strict mypy, and Git whitespace checks passed after the layout change. | The counter is visible from launch as `本次已成功扫描 0 只`; an already-open GUI must be closed and reopened to load the new layout. Synced through f879680 plus the current working tree. |
| 2026-08-26 GUI elapsed timer | Show current-run elapsed time directly below the successful-scan count. | Added `本次已用时间 00:00:00` below the counter. A successful process spawn for `scan-one` or `scan-batch` starts a monotonic clock; the existing 100 ms GUI poll refreshes a second-resolution `HH:MM:SS` display. Completion, failure, and Ctrl+C take a final sample and clear the active start time so the value freezes. A new scan resets it; device checks and dry runs leave the last scan duration intact. | All 108 tests, Ruff, strict mypy, and Git whitespace checks passed. Focused tests cover negative/partial seconds, minute/hour formatting, live monotonic refresh, and stopped-state freezing. | This is display-only and does not change subprocess commands, batch CSV counting, scan timeouts, automation input, or safety verification. Synced through f879680 plus the current working tree. |
| 2026-08-26 shadow/weather move branch | Recover move names when shadow and weather modifier rows shift the move heading above the normal recognition crop. | Kept the normal `(100,1100,1340,2320)` move-anchor OCR as the fast path. Only its miss runs `(100,800,1340,1800)` and accepts the upper layout when both the move-section heading and `暗影獎勵` are OCR-confirmed. The existing move extractor continues to discard `暗影獎勵` and `天氣優勢`; an upper heading without shadow evidence remains a null/fail-closed result. | All 111 tests, Ruff, strict mypy, and Git whitespace checks passed. Focused synthetic tests cover fallback success, missing-shadow rejection, and zero shadow-fallback OCR after a normal anchor succeeds. Saved-real replay returns `冰息`/`遷怒` for shadow 冰雪龍 in 2.730 seconds and `躍起`/`遷怒` for shadow 果然翁 in 2.668 seconds, both with no warnings. A normal 熔蟻獸 remains `火焰旋渦`/`噴射火焰` on the first path in 1.715 seconds. | Future recognition uses the branch; existing `recognition.json` files containing null moves are not rewritten automatically. Page-state, CP/HP, IV, fingerprint, nickname, switch, wrap, and resume validation are unchanged. Synced through f879680 plus the current working tree. |
| 2026-08-26 17:56 JST | Audit whether the new Dynamax 豪力 scan captured every metric. | Inspected complete scan `20260826_175653_424015_75d4fea0c4ef45deb0254beedfaffd17`, its summary/moves/appraisal screenshots, manifest, recognition, rename evidence, move-state JSON, and timing profile. Replayed the upper move ROI without writing scan artifacts. No behavior code changed. | Name `豪力`, CP1123, IV `13/11/12`, and verified nickname `豪力13/11/12` were stored. The screenshot visibly contains HP120/120, 91.05 kg, 1.71 m, Fighting type, Dynamax status, regular moves `踢倒`/`地獄翻滾`, and Max Move cards. Page-state OCR confirmed regular move powers 5/55. Final recognition nevertheless stored both regular moves as null: their heading is around y=990, above the normal y=1100 crop; the shadow fallback sees the upper heading and `極巨招式` but correctly refuses it because `暗影獎勵` is absent. The current schema has no HP, weight, height, type, Dynamax-status, or Max-Move fields. | The scan is complete because moves are nullable, not because every visible metric was structured. A future Dynamax branch must be explicit—e.g. require `極巨招式` separately from shadow evidence—and decide whether Max Moves require new persisted fields. Synced through f879680 plus the current working tree. |
| 2026-08-26 per-Pokémon step profiling | Measure real automatic-scan time by Pokémon and by actionable step. | Added monotonic timing around initialization, page verification, capture, navigation, recognition, finalization, and granular rename operations. With `--debug`, each scan atomically maintains `debug/automation/timings.json`; raised steps are marked failed and the run is finalized as complete, failed, interrupted, or dry-run. Batch inherits one file per attempted Pokémon. | All 96 tests, Ruff, strict mypy, and Git whitespace checks passed. Focused automation assertions cover complete and failed timing artifacts. | Timing no longer relies on screenshot or JSON modification times, and nickname clearing/input/confirmation/verification costs can be compared independently. Synced through f879680 plus the current working tree. |
| 2026-08-26 progressive summary OCR | Reduce repeated summary OCR without weakening the accepted page states or identity comparators. | `detail_summary` now runs the unchanged three ordinary CP variants and three ordinary name variants first, returns immediately for name+CP, runs six bright-background CP variants only after an ordinary CP miss with a valid name, and OCRs HP only after all CP paths fail. A missing name stops without useless CP/HP fallback. Final `read_scan` CP extraction follows the same early-stop rule. CP/HP parsing, fingerprint ROI/distance, nickname checks, page states, switch, wrap, and resume comparators are unchanged. | All 102 tests, Ruff, strict mypy, and Git whitespace checks passed. Focused tests assert zero enhanced-CP/HP calls on the fast path, no fallback when name is absent, and both fallback levels. Six saved summaries retained their known names and user-confirmed CP values at 3.2–3.6 seconds per detection versus roughly 8.6–9.0 seconds in prior live profiles. The corrected `古月鳥` CP path returned `CP1217` from 3 ordinary candidates in 1.905 seconds instead of accepting the old threshold `CP121` overwrite. | Existing historical `recognition.json` and CSV data are not rewritten automatically. Synced through f879680 plus the current working tree. |
| 2026-08-26 remove full-screen stability waits | Make permanent Pokémon/CP animation impossible to use as a transition gate and expose actual OCR cost. | Audited every wait call and found the production moves, menu, appraisal entry/bars/exit, rename keyboard/dialog, and detail-summary paths already use detector-state polling. The only full-screen waiter was reachable solely through unused `_tap()` and `_wait()` methods. Removed that dead chain, its whole-screen pixel-difference utility, 8-second stable config, constructor injection, tests, and debug writer. `timings.json` now reports `stable_wait_seconds` and OCR-engine-only `ocr_seconds` both per step and in total. Final detectors and CP/name/HP/fingerprint/nickname/switch/wrap/resume rules are unchanged. | All 101 tests, Ruff, strict mypy, and Git whitespace checks passed. The live-flow test asserts no stable waiter exists, timing tests assert zero stable time per step, and OCR accumulation has a deterministic engine-time test. Latest pre-change real timing showed 13.39 seconds in appraisal-entry detector polling and 11.60 seconds in appraisal-exit detector polling, with no stability artifact; this confirms the expensive component was OCR, not an 8-second stability timeout. | New timing fields appear on the next debug-enabled scan; historical timing JSON is not rewritten. Synced through f879680 plus the current working tree. |
| 2026-08-26 appraisal greeting gate | Advance the first appraisal dialogue as soon as the visible bottom-left greeting proves the transition succeeded. | Added the fixed Mate 30 ROI `(80,2450,420,2615)` and treats a confidence-0.85-or-higher OCR candidate containing exact `你好` as `appraisal_dialogue`. IV-bar geometry still has first priority. The greeting fast path runs before menu OCR only where the caller's expected-state ordering prioritizes dialogue, so ordinary menu polling does not gain this OCR cost. The existing broad dialogue detector and four-unknown fallback remain. Once detected, the unchanged service path immediately sends its single `(1120,1660)` advance tap. | All 106 tests, Ruff, strict mypy, and Git whitespace checks passed. Focused tests cover the positive result, incorrect text, low confidence, detector priority, and no added ordinary-menu OCR. The first saved entry frame from scan `20260826_123322_191081_f91f29908297412f8f2232bc650c4945` replayed as `appraisal_dialogue`, confidence 0.99997, in 0.50 seconds; its earlier live profile spent 13.39 seconds because generic dialogue OCR did not include `你好`. | This changes only the appraisal-entry readiness signal. Menu-target safety, one-tap limit, IV-bar confirmation, CP/name/HP/fingerprint/nickname checks, switching, wrap, and resume remain unchanged. Synced through f879680 plus the current working tree. |
| 2026-08-26 15:53 JST | Diagnose the latest `怒鸚哥` rename failure. | Inspected scan `20260826_155324_419066_36539320c97040f5a4d4a36285bea714`, its manifest, timings, recognition, actions, and every nickname state record. Replayed the next saved summary through all three name variants. No phone input or behavior code change was made. | Recognition correctly produced `怒鸚哥`, CP577, IV `11/7/8`. After reset, the immediate wait-00 summary state returned only `CP577` and the one-character name `怒`; `_summary_name_from_detection()` accepted it and therefore constructed expected nickname `怒11/7/8`. The source PNG for wait 00 was not persisted, but the next saved full-name frame demonstrates the mechanism: two OCR variants split the row into separate `怒` and `鸚哥` candidates, while only one returned `怒鸚哥`; `_best_text()` chooses one candidate solely by confidence and never joins same-row name tokens. The editor later contained the correct half-width `怒鸚哥11/7/8` at confidence 0.99994, so exact verification correctly stopped before both confirmation taps. | Exit 15 was a fail-closed expected-name construction false negative, not an input, full-width, appraisal, IV, or saved-nickname failure. The editor remained open and no incorrect nickname was committed. A fix should validate the reset name against the already recognized species name or require a dedicated expected default-name transition instead of accepting the immediate generic-summary token. Synced through f879680 plus the current working tree. |
| 2026-08-26 wide default-nickname evidence | Prevent a split generic name token from constructing the post-reset IV nickname. | Removed `_summary_name_from_detection()` from rename expectation construction. After the existing post-reset `detail_summary` gate, reuse the dedicated `(150,1300,1290,1550)` nickname reader, which sorts and joins same-row OCR tokens, to obtain the restored default name. Empty evidence fails before the second editor/input; debug mode records `verified_default_nickname_wide`. The exact width-sensitive editor comparison and final wide-summary comparison remain mandatory. | All 106 tests, Ruff, strict mypy, and Git whitespace checks passed. Focused coverage succeeds when generic summary evidence is only `妙` but wide evidence is `妙蛙花`, and still rejects a wrong final editor candidate. The saved post-reset `怒鸚哥` summary replays as `怒鸚哥` through the wide reader even though two generic variants split it into `怒` and `鸚哥`. | The change removes repeated generic-token dependence without changing CP/HP, static fingerprint, nickname character-width, switch, wrap, or resume validation. Synced through f879680 plus the current working tree. |
| 2026-08-25 identity audit | Define the safety boundary for future batch nickname changes. | Audited the static fingerprint, `SummaryIdentity`, adjacent-row comparison, switch-to-next, wrap, and resume paths. Documented that nickname is outside the hash but remains a mandatory identity field, and that batch rename requires a dedicated expected-transition validator rather than weakening the existing comparators. | Two real before/after rename screenshot pairs returned identical 256-bit hashes and Hamming distance 0. Source inspection confirmed the existing exact-name, CP, HP-when-present, and distance-8 checks; no behavior code or tests were changed. | The implementation boundary is recorded; batch renaming remains unimplemented pending the dedicated post-edit and resume evidence path. Synced through 4a95dc252c0d96a4747d90e20561b19d50eb8ab1 plus the current uncommitted working tree. |
| 2026-08-25 17:07 JST | Diagnose batch resume exit 16 for `inventory.csv`. | Compared the file's 10-column recognition-export header with the exact 13-column batch contract and checked whether its eight scan IDs still had source scan directories. Documented the safe new-file and evidence-backed migration boundary; no CSV or behavior code was changed. | All eight source scan directories were absent, so `page_fingerprint` and `scan_directory` cannot be safely reconstructed. `git diff --check` passed. | The file is intentionally rejected before device input; a new batch must use a new CSV path without `--resume`. Synced through 4a95dc252c0d96a4747d90e20561b19d50eb8ab1 plus the current uncommitted working tree. |
| 2026-08-25 batch rename | Add opt-in verified nickname changes to bounded batch scanning without weakening same-Pokémon guards. | Added `scan-batch --rename-with-iv` and GUI forwarding; exact pre-OK editor text, wide final-name evidence, `renamed_summary.png`, `nickname_change.json`, a dedicated CP/HP/static-fingerprint transition, 16-column before/after/status CSV rows, legacy 13-column non-rename loading, post-rename switch/wrap baselines, and fail-closed renamed resume. Left `same_static_summary`, `_same_switch_identity`, ROI, and distance 8 unchanged. | Full 88-test suite, Ruff, strict mypy, CLI help, Git whitespace checks, and two real final-screenshot wide-ROI replays passed. Tests cover wrong editor text before confirmation, transition field failures, no-row/no-swipe rejection, verified row switching, renamed resume, ambiguous nickname zero input, schema compatibility, and rename-mode consistency. | Batch rename is implemented and saved-image-confirmed for the fixed Mate 30 layout. A live multi-Pokémon rename batch remains pending; existing valid 13-column batches resume only without the rename flag. Synced through 4a95dc252c0d96a4747d90e20561b19d50eb8ab1 plus the current uncommitted working tree. |
| 2026-08-25 18:03–18:16 JST | Diagnose the live batch rename that saved successfully but exited 15 before left swipe. | Inspected both incomplete scan manifests, editor evidence, actions, and final screenshots. The old final-name crop `(150,1250,1290,1600)` read the visible `飄飄球12/2/5` as `飄飄球125` because OCR dropped the middle `2`. Tightened only the dedicated nickname row to `(150,1300,1290,1550)` and added split-token plus missing-character regression coverage; generic OCR, CP/HP, static fingerprint, switch, and resume comparators are unchanged. | Both saved failed screenshots now return tokens `飄飄球12`, `2`, `5` and match expected skeleton `飄飄球1225`; the full 89-test suite, Ruff, strict mypy, and Git whitespace checks passed. | The failure was a fail-closed OCR false negative, not a rename or swipe defect. The earlier runs correctly wrote no CSV row and sent no left swipe. A post-fix live run is still required to confirm append and switch. Synced through 4a95dc252c0d96a4747d90e20561b19d50eb8ab1 plus the current uncommitted working tree. |
| 2026-08-25 18:41–18:49 JST | Diagnose the latest batch item failure after four verified renamed rows. | Read the latest incomplete manifest, recognition, action/state logs, summary screenshot, and `inventory0825.csv`. The scan completed CP140, `拉魯拉絲`, moves, and IV `11/4/1`, then the sole rename tap `(932,1460)` left the page at `detail_summary`; no keyboard, delete, or text input followed. Compared name OCR boxes across the failure and three preceding successes. | The four-character `拉魯拉絲` box ends at x=934, so x=932 is inside its last glyph; successful three-character boxes end at x=886–891. The CSV remains four verified rows and contains no partial fifth row. `git diff --check` passed after the scoped guide sync. | The fixed pencil x-coordinate does not generalize across nickname widths. The failure was safely gated. No behavior code was changed; the recommended repair is a bounded target derived from the verified name box. Synced through 4a95dc252c0d96a4747d90e20561b19d50eb8ab1 plus the current uncommitted working tree. |
| 2026-08-25 18:50–18:56 JST | Replace the width-sensitive fixed nickname-pencil tap. | Removed the fixed edit point from the automation profile. The detail-summary detector now groups confident Chinese/ASCII name-row tokens, targets 46 pixels beyond the rightmost edge, requires the result inside `(850,1350,1240,1580)`, records it in `PageDetection`, and stops before input when unavailable. Dry-run describes the OCR-relative action. Editor, final nickname, CP/HP/fingerprint, switch, and resume checks are unchanged. | The full 94-test suite, Ruff, strict mypy, and Git whitespace checks passed. Eight real edit screenshots replay to bounded targets: the failed `拉魯拉絲` becomes `(982,1454)`, three-character names remain around x=933–937, and long suffixed names reach x=1119–1146. | Dynamic targeting is implemented and saved-image-confirmed with no fixed fallback; a post-fix physical batch retry remains pending. Synced through 4a95dc252c0d96a4747d90e20561b19d50eb8ab1 plus the current uncommitted working tree. |
| 2026-08-25 19:13–19:38 JST | Diagnose why the batch tried to rename an already-correct 泥巴魚. | Inspected the newest incomplete scan, recognition, state/action logs, screenshot, all six CSV rows, wrap code, and pairwise fingerprints. Row 1 and row 6 are the same CP175/fingerprint-0 飄飄球 but generic long-name OCR differed as `"2"` versus `"5"`; the exact-name wrap check missed the cycle. The following page is the same CP903/fingerprint-0 泥巴魚 as verified row 2. Its redundant rename then used `(1146,1453)`, derived from a `NAME_RECT` clipped at x=1100, and missed the pencil. | Both repeated pairs have identical 256-bit fingerprints and CP. The latest page remained `detail_summary`; no delete, text, CSV row 7, or left swipe followed. The wider name row retains the final token through x=1144 and derives x=1190, but no behavior code was changed. `git diff --check` passed after guide sync. | The already-correct nickname is not the defect. The primary defect is renamed wrap detection depending on unstable generic long-name OCR; the clipped dynamic pencil source is a secondary fail-closed issue reached only because of that missed wrap. Synced through 4a95dc252c0d96a4747d90e20561b19d50eb8ab1 plus the current uncommitted working tree. |
| 2026-08-25 20:10–20:18 JST | Fix renamed wrap detection and clipped long-name pencil targeting. | Kept `_same_switch_identity` unchanged. Added a verified-rename-only wrap check requiring the switched screenshot's wide nickname to match the first row's saved expected nickname plus unchanged CP, optional HP, and distance-8 fingerprint; switch now returns its evidence screenshot and records `wrap_comparison_state.json`. Moved pencil target derivation from generic `NAME_RECT` to the existing wide nickname row, still using confidence, row, offset, and lane bounds with no fallback. | Full 97-test suite, Ruff, strict mypy, and Git whitespace checks passed. Real row1/row6 replay changes only the dedicated wrap from false to true (`飄飄球1225` versus `飄飄球12/2/5`, identical CP/fingerprint); the latest 泥巴魚 screenshot derives `(1190,1453)`. Tests reject wrong wide nickname and changed CP, and preserve general generic-name strictness. | Both defects are implemented and saved-real-evidence-confirmed without weakening normal identity comparison. A clean post-fix physical batch run remains pending; the existing CSV already contains the historical duplicate row 6 and is not rewritten automatically. Synced through 4a95dc252c0d96a4747d90e20561b19d50eb8ab1 plus the current uncommitted working tree. |

| 2026-08-26 Shadow/Dynamax classification and CSV remarks | Recover regular moves from both shifted special layouts and preserve the classification in exports. | Kept the normal move crop as the first path. Its miss alone runs the upper crop and accepts it only with an upper move-section anchor plus exactly one high-confidence marker: `暗影獎勵` or `極巨招式`. Modifier and Max Move labels are filtered from regular moves. Recognition warnings add `类型：暗影` or `类型：极巨化`; the existing inventory and batch CSV warning/remarks column serializes them without a header change. | All 113 tests, Ruff, strict mypy, and Git whitespace checks passed. Focused tests cover both special branches, ordinary-path short-circuiting, fail-closed unmarked upper layouts, and both CSV writers. Saved-real replay recovers `冰息`/`遷怒` and `躍起`/`遷怒` for two Shadow scans, `踢倒`/`地獄翻滾` for Dynamax 豪力, and preserves normal `火焰旋渦`/`噴射火焰` with no type remark. | CP/name/HP, IV, fingerprint, nickname, page-state, switch, wrap, and resume safety checks are unchanged. Historical recognition JSON and CSV files are not rewritten automatically. Synced through f879680 plus the current working tree. |

| 2026-08-26 19:16 JST | Diagnose the latest `哈力栗` rename failure. | Inspected scan `20260826_191654_239673_757c091e92d0461d9a31ab735dca6cda`, manifest, timings, actions, nickname states, screenshots, and replayed the exact post-reset wide ROI with OCR. No phone input or behavior code changed. | Scan recognition reached CP605 and IV `14/11/12`. The restored page visibly showed `哈力栗`; wide OCR returned `哈力栗` plus a far-right acquisition-date fragment as `1`, producing internal default `哈力栗1` and expected `哈力栗114/11/12`. The editor then correctly contained half-width `哈力栗14/11/12`. Exact verification failed at `rename_confirm_iv` and sent neither keyboard nor dialog confirmation. Moves were separately nullable after their anchor miss and did not cause the exit. | Exit 15 is a fail-closed expected-name OCR false positive, not a wrong input or saved nickname. No CSV row, `nickname_change.json`, `renamed_summary.png`, or next-Pokémon swipe was produced. A repair should exclude non-name far-right ROI evidence without weakening exact editor/final nickname checks. Synced through f879680 plus the current working tree. |

| 2026-08-26 nickname-row geometry filter | Remove acquisition-date OCR noise without narrowing the wide nickname ROI or weakening exact nickname checks. | Reintroduced configured `nickname_edit_row_tolerance: 70`, anchored the wide reader on the tallest/highest-confidence Chinese candidate (with a tall-near-y1450 no-Chinese fallback), and retained only candidates within that center-y tolerance and at least 40% of anchor height. Kept `(150,1300,1290,1550)`, left-to-right joining, fixed editor taps, half-width equality, final summary, fingerprint, switch, wrap, and resume validation unchanged. | All 114 tests, Ruff, strict mypy, and Git whitespace checks passed. Synthetic coverage reproduces the exact `哈力栗` and date-fragment boxes, independently rejects undersized same-row noise, and preserves split IV tokens. Saved-real replay changes the failed frame from `哈力栗1` to `哈力栗`; completed `古月鳥15/15/13` remains complete. | Future runs use the geometry filter; historical incomplete manifests and CSV data are not rewritten automatically. Synced through f879680 plus the current working tree. |

| 2026-08-26 20:36 JST | Diagnose the immediate batch-resume failure after the nickname-row fix. | Inspected the newest modified artifact, current CSV tail, batch resume code, and replayed `resume_current.png` through the current summary detector plus every CP variant. No phone input or behavior code changed. | No new scan directory was created. The current page visibly showed 索財靈 CP458 and OCR returned name `索財靈` plus `73/73 HP`, but ordinary CP variants returned only `O` and enhanced variants added only low-confidence `000`. The page detector therefore returned `detail_summary` using `name_hp`; batch `summary_identity()` still required name+CP and raised before `resume_state.json`. | This was a fail-closed resume CP-readiness false negative, unrelated to the repaired `哈力栗1` nickname row. No scan, CSV row, or swipe was sent. A future repair should poll for a reliable name+CP resume identity rather than weakening CP into optional HP-only identity. Synced through f879680 plus the current working tree. |

| 2026-08-26 resume partial identity and gold-background CP | Let a valid current detail page pass resume readiness without weakening any normal identity comparator. | Added an internal `ResumeObservation` with optional CP, name-first different-page proof, two CP-only 500 ms retries guarded by unchanged static fingerprint, and a same-name-only strong fallback requiring generic/wide name, exact HP, distance-8 fingerprint, plus saved expected nickname for verified rows. The strong fallback permits one left swipe without a retry. Added HSV near-white CP OCR between ordinary and threshold paths; bare digits require confidence 0.95 and full-label CP-band geometry. `SummaryIdentity.cp` remains `int`; ordinary switch/duplicate/rename/wrap/append code is unchanged. | All 128 tests, Ruff, strict mypy, and Git whitespace checks passed. Synthetic tests reject isolated anniversary `10`, weak/broad `000`, HP mismatch, nickname mismatch, and changed-page retry evidence. The local ignored real screenshot regression returns `detail_summary`, `索財靈`, `73/73HP`, and `CP458`. A read-only real `0826.csv` resume replay returned ready-to-scan-current with zero swipes. | The 20:36 resume false negative is repaired for future runs. Existing CSV and incomplete artifacts are preserved. Synced through f879680 plus the current working tree. |

| 2026-08-26 23:34 JST | Diagnose the latest first-row batch stop after a verified `索財靈` rename. | Inspected scan `20260826_233228_919189_a8e5656b21534ee4bba19857387be16d`, its complete manifest, recognition/nickname evidence, CSV row, action log, before-switch state and screenshot, and replayed all CP OCR paths. No device input or behavior code changed. | The original and post-rename blue-background frames both recover CP458, and the rename transition preserves CP, HP, and fingerprint. After the configured delay, `before_switch.png` caught the animated anniversary gold frame: name `索財靈10/15/15` and `73/73HP` were valid, but ordinary and HSV CP OCR returned nothing while threshold variants returned only `O` and low-confidence `0`. The page detector accepted `detail_summary` through `name_hp`, then strict `summary_identity()` rejected the missing CP before `_same_switch_identity`; no switch-attempt artifact or swipe exists. | Row 1 is safely and durably saved in `0826.csv`; the stop is a post-save, pre-swipe CP-readiness false negative. The resume-specific partial observation does not apply to ordinary `_switch_to_next`, whose strict name+CP/fingerprint guard remains unchanged. Synced through f879680 plus the current working tree. |

| 2026-08-27 verified-rename pre-switch CP fallback | Allow one safe left swipe when an animated event frame hides CP after a fully verified rename. | Kept `_same_switch_identity`, normal rows, duplicate, rename-transition, wrap, and append checks unchanged. A verified renamed pre-switch CP miss now takes two 500 ms CP-only frames guarded against both the first retry frame and saved baseline by the existing distance-8 static threshold. CP recovery returns to strict name+CP/fingerprint. A total miss rereads the latest frame and requires the saved expected wide nickname, exact HP from `renamed_summary.png`, and the same fingerprint threshold, then permits only the first left swipe with no stronger retry. Added complete debug evidence for retries, proof, action, and result. | All 135 tests, Ruff, strict mypy across 19 source files, and Git whitespace checks passed. Focused tests pass CP recovery and the one-swipe strong fallback, and reject wrong nickname, HP mismatch, missing baseline HP, changed static page, and all unrenamed CP misses with zero input. Read-only replay of real scan `20260826_233228_919189_a8e5656b21534ee4bba19857387be16d` returns nickname `索財靈10/15/15`, exact `73/73HP`, and fingerprint distance 0 between the gold current frame and saved blue baseline. | The observed post-save false negative is repaired without making CP optional in `SummaryIdentity` or changing general identity comparison. A new physical multi-item run remains pending. Synced through f879680 plus the current working tree. |

| 2026-08-27 daily debugging summary | Consolidate every issue and change from the 2026-08-26 continuous debugging session. | Added a standalone Chinese report covering 14 problems across input/rename, OCR/performance, target-state polling, timing/GUI, Shadow/Dynamax layouts, gold-background resume, and verified-rename switching. Recorded safety invariants, real scan evidence, compatibility limits, file routes, and remaining physical checks; linked it from the repo-docs reader routes. | Cross-checked the report against the current source, focused walkthroughs, real scan IDs, and this change log; Markdown links and Git whitespace are checked in the final verification. | The day now has one readable handoff document instead of requiring reconstruction from chronological change-log entries. Synced through f879680 plus the current working tree. |

Synced through f879680 plus the current working tree.
