# Source evidence for local capture and dataset paths

> This is lookup material. Read the walkthrough first if you do not yet
> understand the behavior.

## Understanding brief

The representative behavior is `pokemon-go-cleanup capture`. CLI options and
environment-backed configuration enter first. A successful run produces one PNG
and one JSON sidecar below `data/screenshots/YYYY-MM-DD/`.

The hard part is the trust boundary around ADB. The command may be missing,
several devices may be attached, a phone may be unauthorized, or the screenshot
subprocess may return an error or non-PNG bytes. The code resolves those states
before it writes a capture.

The strongest boundary checks are the selection branches in `select_device()` and
the PNG signature check in `capture_screen()`. The tests falsify the happy-path
model with missing, unauthorized, multiple-device, command-failure, and invalid
PNG cases. Eight scan groups produced on the target Huawei Mate 30 were later
fully decoded by the dataset validator; that confirms the guided device path,
but not every possible native Windows ADB installation.

A careful newcomer will likely ask whether the JSON and PNG are always written as
an atomic pair. The current answer is no: a storage failure during the second
write can leave the PNG without its JSON sidecar. The CLI reports that failure as
exit code 11.

## Guided scan extension

The newer `pokemon-go-cleanup scan-one --guided` path groups `summary.png`,
`moves.png`, and `appraisal.png` under one dated scan ID from exactly one device.
It writes `manifest.json` before prompting and refreshes it after each success.

Its hard boundary is a midway failure: earlier screenshots must remain, temporary
files must be cleaned, and the manifest must become `incomplete` with the exact
failed step. Fake-runner integration tests exercise the complete ADB command path
without a physical phone or UI automation.

## Dataset and annotation extension

`dataset validate` recursively finds scan-shaped directories, fully decodes
present PNGs, compares their dimensions, and parses manifests and annotations
through strict Pydantic models. Missing artifacts remain distinguishable from
corrupt or malformed artifacts. `dataset status` exposes the same model without
turning an invalid scan into a command failure.

`annotate` prints all screenshot paths and collects manual ground truth without
OCR or device commands. Existing work requires confirmation or `--force`;
non-interactive JSON input is typed before a same-directory atomic replacement.

## Fixed automatic scan extension

`scan-auto-one` is a single-run Huawei Mate 30 state machine. The fixed profile
requires 1440x3120 and checks a fresh expected state before every input. Every
post-action transition polls the approved detector states; no production path
uses whole-screen pixel stability. Menu opening polls OCR for the explicit
`action_menu` target every 500 ms for up to 10 seconds, with one
detail-state-confirmed retry. Dry-run records the initial state and coordinate
plan without calling tap, swipe, or BACK.

The appraisal row has no guessed coordinate: OCR must reliably locate
`調查寶可夢`, and the derived center must remain outside the transfer band and
away from an OCR `傳送` center. Existing OpenCV IV geometry confirms the bars
before appraisal capture. Failures preserve final screenshots and record the
current automation step in an incomplete manifest. A later real Mate 30 run
completed all three captures, appraisal progression, safe exit, recognition, and
a complete manifest.

## Bounded batch extension

`scan-batch` delegates each item to the same one-scan service. Its normal mode
adds only a centralized pair of fixed left swipes, each permitted after a fresh
`detail_summary` matches the completed identity. With `--rename-with-iv`, a
separate transition validator permits only the exact planned nickname while CP,
optional HP, and the static fingerprint remain; the general name/CP/fingerprint
comparators and threshold are unchanged. The next summary must change name, CP,
or static fingerprint; polling lasts 15 seconds per attempt and allows one normal
left swipe plus one stronger left retry, never a right swipe. Each complete result
atomically rewrites the CSV before switching. Resume accepts only rows whose
referenced manifests and any verified nickname evidence remain complete.

## Evidence Traversal Log

| Pass | Purpose | Inspected evidence | What changed in the model |
| --- | --- | --- | --- |
| Pass 1 | Map the main path | [`pyproject.toml`](../../pyproject.toml), [`cli.py`](../../src/pokemon_go_cleanup/cli.py), [`adb.py`](../../src/pokemon_go_cleanup/adb.py), [`capture.py`](../../src/pokemon_go_cleanup/capture.py), [`models.py`](../../src/pokemon_go_cleanup/models.py) | Confirmed the command-to-device-to-PNG-to-JSON handoffs and the dated output location. |
| Pass 2 | Challenge the happy path | [`exceptions.py`](../../src/pokemon_go_cleanup/exceptions.py), [`config.py`](../../src/pokemon_go_cleanup/config.py), [`logging_config.py`](../../src/pokemon_go_cleanup/logging_config.py), [`test_adb.py`](../../tests/test_adb.py), [`test_capture.py`](../../tests/test_capture.py), [`test_cli.py`](../../tests/test_cli.py) | Added the state-selection failures, PNG trust check, Unicode path coverage, structured error logging, exact exit codes, and the non-atomic pair caveat. |
| Pass 3 | Trace the guided lifecycle | [`adb_runner.py`](../../src/pokemon_go_cleanup/adb_runner.py), [`scan.py`](../../src/pokemon_go_cleanup/scan.py), [`guided_prompts.py`](../../src/pokemon_go_cleanup/guided_prompts.py), [`test_scan.py`](../../tests/test_scan.py), [`test_scan_cli.py`](../../tests/test_scan_cli.py) | Added the three user handoffs, atomic step files, progressive manifest, incomplete recovery, option coverage, and no-tap/swipe command evidence. |
| Pass 5 | Trace the fixed automatic safety boundary | [`automation.py`](../../src/pokemon_go_cleanup/automation.py), input wrappers in [`adb.py`](../../src/pokemon_go_cleanup/adb.py), [`recognition.py`](../../src/pokemon_go_cleanup/recognition.py), [`test_automation.py`](../../tests/test_automation.py), and [`test_adb.py`](../../tests/test_adb.py) | Added eight page states, centralized coordinates, dry-run zero-input evidence, one moves scroll, no-scroll menu opening with OCR target-state polling and one gated retry, transfer clearance, one appraisal-dialogue tap followed by 500 ms/30-second IV-bar polling with no retry, IV confirmation, a center tap to exit appraisal, optional OCR-gated nickname reset, one complete ASCII-suffix `input text` request, width-sensitive pre-confirmation editor verification, timeouts, and incomplete recovery. |
| Pass 4 | Trace dataset trust and annotation replacement | [`dataset.py`](../../src/pokemon_go_cleanup/dataset.py), [`annotation.py`](../../src/pokemon_go_cleanup/annotation.py), [`storage.py`](../../src/pokemon_go_cleanup/storage.py), [`test_dataset.py`](../../tests/test_dataset.py), [`test_dataset_cli.py`](../../tests/test_dataset_cli.py), [`test_annotation.py`](../../tests/test_annotation.py) | Added recursive discovery, full image decoding, complete/incomplete/invalid classification, typed ground truth, UTF-8 persistence, overwrite protection, and runtime-only synthetic image evidence. |
| Pass 6 | Trace bounded batch durability and switching | [`batch.py`](../../src/pokemon_go_cleanup/batch.py), reused [`automation.py`](../../src/pokemon_go_cleanup/automation.py), CLI wiring in [`cli.py`](../../src/pokemon_go_cleanup/cli.py), atomic persistence in [`storage.py`](../../src/pokemon_go_cleanup/storage.py), and [`test_batch.py`](../../tests/test_batch.py) | Added a fixed Huawei horizontal gesture, post-exit summary gate, strict name/CP change and wrap checks, two-attempt ceiling, per-row atomic CSV, complete-manifest resume checks, switch debug evidence, and a separate expected-nickname transition/resume path that does not change the general identity comparators. |

## Claim ledger

| Claim | Evidence | Confidence | Caveat | Used by |
| --- | --- | --- | --- | --- |
| The installed entrypoint is `pokemon-go-cleanup`. | [`[project.scripts]`](../../pyproject.toml) maps the command to `pokemon_go_cleanup.cli:app`; the installed `--help` smoke check returned the command tree. | Confirmed | The smoke check ran in WSL, not native Windows. | Guide README, walkthrough |
| Runtime configuration is validated by Pydantic and can come from `POKEMON_GO_CLEANUP_*` environment variables. | [`AppConfig`](../../src/pokemon_go_cleanup/config.py) subclasses `BaseSettings`; [`main()`](../../src/pokemon_go_cleanup/cli.py) preserves environment/default values for omitted flags; [`test_config.py`](../../tests/test_config.py) covers both precedence paths. | Confirmed | Explicit CLI flags override matching environment values. | Walkthrough |
| ADB is discovered from an explicit path or `PATH`. | [`discover_adb()`](../../src/pokemon_go_cleanup/adb.py) checks a configured file/directory and then `shutil.which`. | Confirmed | A real Windows path has not yet been exercised in this workspace. | Walkthrough |
| An implicit capture requires exactly one ready device. | [`select_device()`](../../src/pokemon_go_cleanup/adb.py) rejects zero ready devices, unauthorized-only devices, and more than one ready device; [`test_adb.py`](../../tests/test_adb.py) covers each branch. | Confirmed | If exactly one ready device and additional unauthorized devices exist, the ready device is selected. | Walkthrough |
| The current logical resolution is the last size reported by `adb shell wm size`. | [`get_resolution()`](../../src/pokemon_go_cleanup/adb.py) selects the last `WIDTHxHEIGHT` match; `test_resolution_uses_current_override` covers physical plus override output. | Confirmed | An unexpected vendor-specific output becomes exit code 10. | Walkthrough |
| Screenshot bytes must start with the PNG signature before persistence. | [`capture_screen()`](../../src/pokemon_go_cleanup/adb.py) rejects empty and non-PNG output; two tests cover invalid bytes and ADB command failure. | Confirmed | The code validates the signature, not full PNG structure. | Guide README, walkthrough |
| Captures use a dated directory and Windows-safe generated filename. | [`CaptureService.capture()`](../../src/pokemon_go_cleanup/capture.py) combines local date, microseconds, sanitized serial, and random suffix; [`test_capture.py`](../../tests/test_capture.py) verifies the dated Unicode path and sanitized filename. | Confirmed | The data directory itself may contain Unicode; the serial component is reduced to ASCII-safe characters. | Walkthrough |
| The JSON sidecar records capture time, serial, resolution, both paths, byte size, and SHA-256. | [`CaptureMetadata`](../../src/pokemon_go_cleanup/models.py) defines the contract; `test_capture_saves_dated_png_and_utf8_json` verifies serialized values. | Confirmed | Paths are absolute paths as seen by the runtime that performed the capture. | Guide README, walkthrough |
| A guided scan stores three fixed PNG names and one manifest below `data/scans/YYYY-MM-DD/<scan_id>/`. | [`GuidedScanService`](../../src/pokemon_go_cleanup/scan.py) fixes the step order and paths; [`ScanManifest`](../../src/pokemon_go_cleanup/models.py) defines timestamps, filenames, device data, version, mode, notes, status, and failed step. | Confirmed | Incomplete scans contain only filenames and timestamps for successful steps. | Guided walkthrough, root README |
| Guided screenshot and manifest writes use same-directory temporary files and atomic replacement. | [`storage.py`](../../src/pokemon_go_cleanup/storage.py) flushes, `fsync`s, replaces, and cleans temporary paths; `test_atomic_screenshot_write_removes_temporary_file_on_failure` exercises cleanup through the scan wrapper. | Confirmed | Atomic replacement follows the guarantees of the local filesystem. | Guide README, guided walkthrough |
| A midway guided failure preserves earlier PNGs and marks the manifest incomplete with the failed step. | [`_raise_incomplete()`](../../src/pokemon_go_cleanup/scan.py) and [`GuidedScanStepError`](../../src/pokemon_go_cleanup/exceptions.py) implement recovery; `test_guided_scan_preserves_success_and_marks_midway_failure` verifies a moves failure. | Confirmed | If the manifest itself cannot be rewritten, the CLI explicitly reports that second failure. | Guided walkthrough |
| Missing `--guided` always reports that exact option and exits 2. | [`scan_one()`](../../src/pokemon_go_cleanup/cli.py) emits a fixed plain-text error before exiting; [`test_scan_cli.py`](../../tests/test_scan_cli.py) asserts both the text and code. | Confirmed | This deliberately avoids Rich usage wrapping and terminal-width truncation. | CI, guided walkthrough |
| Dataset validation recursively finds candidates, fully decodes PNGs, and requires equal dimensions. | [`dataset.py`](../../src/pokemon_go_cleanup/dataset.py) implements discovery and Pillow loading; [`test_dataset.py`](../../tests/test_dataset.py) covers nested, corrupt, and mismatched cases. | Confirmed | Candidate detection is artifact- or dated-directory-based. | Dataset walkthrough, root READMEs |
| Dataset status separates complete, incomplete, and invalid scans. | [`validate_scan_directory()`](../../src/pokemon_go_cleanup/dataset.py) classifies absence/lifecycle separately from corrupt present data; [`test_dataset_cli.py`](../../tests/test_dataset_cli.py) verifies the table and validate-only nonzero exit. | Confirmed | A malformed existing annotation makes an otherwise complete scan invalid. | Dataset walkthrough |
| Ground truth is typed, readable UTF-8, atomic, and overwrite-protected. | [`GroundTruth`](../../src/pokemon_go_cleanup/models.py) defines all fields and numeric/IV rules; [`AnnotationService`](../../src/pokemon_go_cleanup/annotation.py) validates input and writes atomically; [`test_annotation.py`](../../tests/test_annotation.py) covers Unicode, Enter-to-keep, force, refusal, and cleanup. | Confirmed | `--input-json` requires `--force` when a destination already exists. | Dataset walkthrough, root READMEs |
| Tests contain no real scan media. | [`test_dataset.py`](../../tests/test_dataset.py), [`test_dataset_cli.py`](../../tests/test_dataset_cli.py), and [`test_annotation.py`](../../tests/test_annotation.py) create synthetic solid-color PNGs under pytest temporary paths at runtime; ignored `data/` remains outside Git. | Confirmed | Local real data is still processed when the user runs the commands. | Privacy boundary |
| Expected failures have stable CLI exit codes and structured error logs. | [`exceptions.py`](../../src/pokemon_go_cleanup/exceptions.py) defines codes 2–16; [`_abort()`](../../src/pokemon_go_cleanup/cli.py) logs error type/code and exits; tests exercise command-specific failures. | Confirmed | Ctrl+C from automatic or batch scanning uses exit 130 after preserving completed local state. | Guide README |
| A bright or saturated event background cannot by itself invalidate a real detail summary. | [`HuaweiMate30PageDetector`](../../src/pokemon_go_cleanup/automation.py) accepts name+CP or name+fixed-ROI HP and records `summary_evidence`. [`RecognitionService._ocr_variants()`](../../src/pokemon_go_cleanup/recognition.py) now runs ordinary color, sharpened, and CLAHE RapidOCR calls progressively: CP stops only on the existing prefix/confidence/positive-value/CP-band conjunction; name stops only on one confident Chinese-bearing token of at least two Chinese characters, never a single-character or same-variant split-token result. An ordinary miss still runs one HSV near-white mask, then all six threshold variants, and HP only after every CP path fails. HSV still requires a CP-band prefixed candidate or a confidence-0.95 bare digit candidate with the wide/tall geometry of the complete CP label; isolated background digits and low-confidence `000` remain rejected. | Source-, test-, saved-real-image-, and physical-device-confirmed | Known ordinary summaries retain their values, including `古月鳥` CP1217 and `怒鸚哥` CP577. Real `resume_current.png` for 索財靈 still follows HSV and yields `CP458` with `73/73HP`. On the same Mate 30 rename-enabled batch path, three ordinary scans before/after measured median total 87.479→79.369 s, cumulative OCR 44.192→29.359 s, and initial summary detection 4.057/3.115 OCR→1.913/0.873 OCR s. | Automatic walkthrough |
| A Huawei Mate 30 running HarmonyOS accepts the guided capture sequence. | The user reported the working physical workflow; the [project context](../../docs/codex-context.md) records the 2026-07-28 read-only validation of eight complete groups with 24 decodable PNGs and eight valid manifests. | Confirmed | This confirms the target guided path, not every native Windows ADB installation or standalone capture setup. | Guide README, walkthroughs |
| Dry-run cannot send ADB input, and live inputs are gated by expected states. | [`AutoScanService`](../../src/pokemon_go_cleanup/automation.py) separates the dry-run return before every input helper; [`test_automation.py`](../../tests/test_automation.py) asserts zero inputs for dry-run/unknown start, the five normal live inputs, no reverse scroll, and the two-attempt menu ceiling. | Confirmed | Fake ADB proves all branches; the first menu-tap path also succeeded on the connected device. | Automatic walkthrough |
| Appraisal requires OCR `調查寶可夢`, positive entry evidence, and IV bars. | [`HuaweiMate30PageDetector`](../../src/pokemon_go_cleanup/automation.py) derives and safety-checks the menu target and reuses the IV detector. After the menu-row click it accepts direct IV geometry or high-confidence `你好` from the fixed bottom-left `(80,2450,420,2615)` ROI as the explicit dialogue-entry state; only then does the service immediately send its one existing dialogue-advance tap. The wider dialogue detector and bounded menu-disappearance fallback remain fail-safe alternatives. [`RecognitionService._ivs()`](../../src/pokemon_go_cleanup/recognition.py) keeps the calibrated y=2150 geometry first; only its miss retries from y=2100 with unchanged bar height, spacing, endpoint color, and quantization, accepting exactly one legal sequence. | Source-, test-, and saved-real-image-confirmed | The first saved entry frame from scan `20260826_123322_191081_f91f29908297412f8f2232bc650c4945` returned `你好`, confidence 0.99997, and `appraisal_dialogue` in 0.50 seconds. In shifted 来悲茶 scan `...bcea62f2`, the old top clipped the y=2136 attack bar; all nine saved wait frames now return `14/11/14` through `upward_fallback`, while seven saved menu/dialogue frames remain negative. Normal saved appraisal `...79ce89e3` returns `12/13/14` with no fallback call. | Automatic walkthrough |
| Appraisal exit uses a dedicated no-OCR return detector. | [`HuaweiMate30PageDetector.detect_returned_from_appraisal()`](../../src/pokemon_go_cleanup/automation.py) requires negative IV-bar geometry plus positive small-ROI teal-disk/ring geometry at both fixed detail close/menu buttons. `_exit_appraisal()` retains the single `(720,1560)` tap, 500 ms polling, 15-second timeout, and failure artifacts. Uncertain frames stay `unknown`; only the final timeout frame may call the existing full detector once for diagnostic evidence. | Source-, synthetic-test-, saved-real-image-, and physical-device-confirmed | Tests reject visible bars, black, full-teal action menu, and one-button animation evidence. Three saved before/after/action-menu triplets replay as `unknown`/`detail_returned`/`unknown` with zero OCR. Same-device medians changed from 6.302 s/3.597 OCR to 2.024 s/0 OCR for `exit_appraisal`; total changed 79.369→75.235 s and total OCR 29.359→27.281 s. | Automatic walkthrough |
| Automatic live finalization reuses only complete recognition evidence from the exact persisted frames. | [`PageDetection`](../../src/pokemon_go_cleanup/automation.py) may carry summary candidates, move candidates/remarks, or IV bars plus the original debug images and PNG SHA-256. [`RecognitionService.read_evidence()`](../../src/pokemon_go_cleanup/recognition.py) re-hashes `summary.png`, `moves.png`, and `appraisal.png`, applies the same best-candidate and result-field conversion, writes the same `recognition.json` and debug image set, and returns `None` on a missing/mismatched frame or incomplete evidence; [`AutoScanService`](../../src/pokemon_go_cleanup/automation.py) then calls the unchanged `read_scan()` fallback. | Source-, synthetic-test-, semantic-equivalence-test-, Shadow/Dynamax-regression-, and physical-device-confirmed | The fast path is reachable only from automatic live capture. Historical, CLI read, guided/manual, incomplete, replay, and fallback flows retain `read_scan()`. Three Mate 30 rename-enabled runs had zero OCR in `recognize_scan`; same-frame old-path replay measured median 2.692 s and 2.481 OCR seconds versus 0.046 s and 0 OCR on the live fast path. | Automatic walkthrough |
| Conflicting pre-rename CP checkpoints require bounded CP-only consensus before editor input. | The first editor pre-state reuses its normal `detail_summary` detection. If its canonical prefixed CP conflicts with `RecognitionResult.cp`, [`AutoScanService._resolve_rename_cp_disagreement()`](../../src/pokemon_go_cleanup/automation.py) leaves the page untouched and captures at most six CP-only frames; one complete CP must repeat twice. Success updates the returned recognition and atomically rewrites `recognition.json`; exhaustion raises before the nickname-row tap. An agreeing or unavailable checkpoint adds no CP-only OCR. | Source- and synthetic-test-confirmed; saved-incident-diagnosed | The retry loop calls only `read_summary_cp()`: it does not repeat name/nickname, HP, fingerprint, or full page detection on every frame. Tests reproduce the incident pattern as baseline 67, checkpoint 97, then CP-only `997 / missing / 997`, and prove a non-repeating burst sends no editor tap. Saved scan `...116b76f...` contains the motivating 67/97/997/99 disagreement but predates the burst, so it cannot independently prove consensus. | Automatic walkthrough |
| A clipped move-section heading is not required for `detail_moves`. | [`HuaweiMate30PageDetector`](../../src/pokemon_go_cleanup/automation.py) first reuses existing move extraction, then pairs a confident Chinese move name with a 1–3 digit right-side damage value on the same fixed Mate 30 row; the heading adds confidence only. Focused synthetic tests reject appraisal labels, and saved real move/appraisal screenshots verify the positive and negative boundary. | Confirmed | This is intentionally calibrated only for the current 1440x3120 Traditional Chinese layout. | Automatic walkthrough |
| Shadow/weather and Dynamax move layouts use dedicated recognition branches. | [`RecognitionService._moves()`](../../src/pokemon_go_cleanup/recognition.py) retains the normal `(100,1100,1340,2320)` anchor OCR first. Only its miss runs `(100,800,1340,1800)` and requires both an upper “道館/團體戰” anchor and exactly one high-confidence marker: `暗影獎勵` or `極巨招式`. Existing label filtering excludes modifier and Max Move labels from regular move names. Accepted results append `类型：暗影` or `类型：极巨化` to warnings; both inventory and batch CSV writers already serialize that field. Synthetic tests cover both positive branches, reject an unmarked upper anchor, and verify both CSV paths. | Source-, test-, and saved-real-image-confirmed | Saved shadow scans `20260826_172102_435208_851e67abcd5d45ac83c77431dab95f0b` and `20260826_172258_454432_70ea51a680a04490982a9c49f03f6c35` replay as `冰息`/`遷怒` and `躍起`/`遷怒`, each with `类型：暗影`. Dynamax 豪力 scan `20260826_175653_424015_75d4fea0c4ef45deb0254beedfaffd17` replays as `踢倒`/`地獄翻滾` with `类型：极巨化`. A normal saved 熔蟻獸 still returns `火焰旋渦`/`噴射火焰` without a type remark through the first path. Historical recognition JSON/CSV is not rewritten automatically. | Automatic and batch walkthroughs |
| Every automatic transition is target-state-driven rather than whole-screen-stability-driven. | Moves polls for `detail_moves`; menu polls for `action_menu`; appraisal entry/bars and nickname keyboard/dialog/summary poll their explicit approved states. Appraisal exit polls the narrower `detail_returned` state backed by absent IV geometry plus both fixed detail-button geometries. The unreachable `_tap()`/`_wait()` chain, full-screen pixel-difference utility, stable-wait configuration, and debug artifact writer were removed. Timing JSON records `stable_wait_seconds: 0` per step and in total. | Source- and test-confirmed | No transition treats animation or mere overlay disappearance as success. The specialized exit state conveys no Pokémon identity and is not used by batch, rename, resume, wrap, or fingerprint comparison. | Automatic walkthrough |
| Resume uses a partial current observation without making strict identity CP optional. | [`ResumeObservation`](../../src/pokemon_go_cleanup/batch.py) holds reliable name, optional CP/HP, fingerprint, and wide nickname only inside `_prepare_resume_position()`. Two agreeing name readers can prove the current page differs before CP; same-name CP misses get two CP-only retries whose static ROI must remain unchanged. A total miss may match the last row only through generic/wide name, exact HP, and the existing distance-8 fingerprint, including saved expected nickname for verified rows. The no-CP switch path permits one gesture only. `SummaryIdentity.cp` remains a required `int`; the generalized fallback reuses a known baseline CP rather than making it optional. | Source-, test-, and saved-real-image-confirmed | Focused tests cover different-name/no-CP/no-swipe continuation, retry recovery, strong fallback success, HP mismatch, verified nickname mismatch, and zero-input aborts. Real `0826.csv + resume_current.png` replay validates the stored last row, recognizes current 索財靈 as different, sends no swipe, and becomes ready to scan. | Batch walkthrough |
| Batch CSV is durable before switching, and switching is bounded by identity checks. | [`BatchScanService`](../../src/pokemon_go_cleanup/batch.py) appends through `atomic_write_text` immediately after a complete one-scan result, then requires a matching post-exit summary before the configured left gestures: `(1180,1500)` to `(260,1500)` over 600 ms, then `(1300,1500)` to `(140,1500)` over 850 ms. Every known-identity CP miss uses one fail-closed rule: ordinary pages require exact generic and wide names, renamed pages require the saved expected nickname, and both require exact HP plus distance-8 fingerprint. Live pre-switch checks first take two static-fingerprint-guarded CP-only retries; only a total strong match may send the first gesture, never the retry. Switch polling can reuse baseline CP only while the same conjunction proves the page is unchanged; a different/new page must yield CP. [`test_batch.py`](../../tests/test_batch.py) covers strict recovery, ordinary and renamed fallbacks, wait polling, and all no-input negatives. | Source-, test-, and saved-real-image-confirmed | The direction is operator-confirmed for the fixed Mate 30 profile; no right-swipe fallback is implemented. The real gold-frame `before_switch.png` for 索財靈 matches `索財靈10/15/15`, `73/73HP`, and fingerprint distance 0 against its blue saved baseline. | Batch walkthrough |
| Nickname mutation is opt-in and batch-safe through dedicated transitions. | `--rename-with-iv` in [`cli.py`](../../src/pokemon_go_cleanup/cli.py) enables [`AutoScanService`](../../src/pokemon_go_cleanup/automation.py) only after IV recognition. Both editor openings require `detail_summary` and tap the fixed nickname-row center `(720,1460)`. After reset, generic summary OCR remains only a page-state gate; the separate `(150,1300,1290,1550)` reader joins left-to-right name tokens and supplies the default nickname used to build the expected result. `build_iv_nickname()` uses Python `len()` over the complete Unicode string: it keeps `name + A/D/H` through 12 characters, otherwise uses six fixed-width ASCII digits `AADDHH`; a compact result over 12 fails before the second editor/input without truncation. Exact width-sensitive editor/final text, CP or a known-baseline CP fallback, exact HP for that fallback, and unchanged distance-8 fingerprint gate persistence. [`BatchScanService`](../../src/pokemon_go_cleanup/batch.py) delegates to this same one-scan transaction and additionally wraps a verified rename batch only when the switched screenshot's wide nickname equals the first row's saved nickname and its CP/HP/fingerprint match. Tests cover pretty/compact selection, zero padding, Unicode length, compact overflow, truncated compact editor text, the fixed tap boundary, nickname/CP/HP/fingerprint negatives, durable rows, renamed resume, CP-missing pre-append transition/restoration, and one-swipe fallback. | Source-, synthetic-test-, and saved-real-image-confirmed | `毛崖蟹11/11/15` remains pretty. `赫拉克羅斯15/14/13` now selects `赫拉克羅斯151413`; editor OCR `赫拉克羅斯15141` still fails before keyboard confirmation and game `OK`. The saved `怒鸚哥` frame still uses the complete wide default name, and real 毛崖蟹 scan `20260827_104456_757116_2a0406bc2bc0430aa724e4c5591635bf` retains exact nickname/HP/fingerprint fallback evidence. | Automatic and batch walkthroughs |
| Final wide-nickname verification retries raw color only after a sharpened mismatch. | [`read_summary_nickname_for_expected()`](../../src/pokemon_go_cleanup/automation.py) runs the existing sharpened 1.5x wide ROI first and immediately returns on the unchanged skeleton match. Only a mismatch runs raw color 2.0x over the same `(150,1300,1290,1550)` ROI; both paths reuse `_join_summary_nickname_row()` for the same anchor, geometry, and token joining. `_verify_final_summary_nickname()` remains the sole authorization and requires the unchanged complete skeleton comparison. | Source-, synthetic-test-, and saved-real-image-confirmed | Real `一對鼠14/15/15` and `一對鼠15/14/12` frames preserve their sharpened `-對鼠...` misreads, while raw color 2.0x returns each exact expected value. Tests prove no raw call on first-pass success and rejection of wrong or prefix-only raw values. No character substitution, fuzzy/prefix authorization, generic-summary fallback, or normalization change exists. | Automatic walkthrough |
| The wide default-name ROI excludes off-row and undersized UI evidence without narrowing long names. | [`read_summary_nickname()`](../../src/pokemon_go_cleanup/automation.py) anchors on the tallest/highest-confidence Chinese candidate, with a tall-near-y1450 fallback when none contains Chinese. It retains only candidates within the configured 70-pixel center-y tolerance and at least 40% of anchor height, then sorts and joins that row. | Source-, test-, and saved-real-image-confirmed | Scan `20260826_191654_239673_757c091e92d0461d9a31ab735dca6cda` now returns `哈力栗`, rejecting acquisition-date noise `1` whose center differs by 104 pixels and height is 32% of the anchor. Completed saved `古月鳥15/15/13` still returns its full nickname; synthetic split-token coverage retains `飄飄球1225` and rejects same-row undersized noise independently. The ROI remains `(150,1300,1290,1550)`. | Automatic walkthrough |
| Transfer, other gameplay mutations, account access, private APIs, traffic inspection, and credentials are outside the implementation. | Batch adds only one fixed horizontal swipe between complete single scans; the existing bounded menu/appraisal inputs remain unchanged. Root [`README.md`](../../README.md) states the boundary. | Confirmed | Any future input expansion requires a new safety and privacy review. | Guide README |

## Coverage and exclusions

The guide now traces standalone `capture`, guided `scan-one`, fixed
`scan-auto-one`, bounded `scan-batch`, calibrated reading, dataset validation,
and manual annotation.
`device list` and `device info` remain adjacent entry paths. Packaging, logging,
configuration precedence, and CI were checked where they constrain these paths.
No real device media is stored in the repository.

The guide does not cover transfer or other gameplay mutations beyond the
documented opt-in automatic nickname reset/suffix, network inspection, account
access, or credentials. One-Pokémon automation, batch row durability, and right
switching are physically confirmed. The fixed detail-moves fallback is
real-image-confirmed, and the resumed two-complete-row device run finished with
two distinct identities.

## Falsifying checks

- `pytest` would invalidate the documented device-state, PNG, manifest,
  annotation, temporary-file cleanup, classification, or failure-recovery
  behavior if those branches changed.
- `ruff check .` and `mypy` catch code-quality and type-contract drift but do not
  prove ADB integration.
- The CI matrix checks Ubuntu and Windows on Python 3.12 and 3.13. Physical ADB
  behavior still depends on the host installation, authorization state, vendor
  `wm size` response, and captured bytes.

Evidence status: Confirmed unless noted.
